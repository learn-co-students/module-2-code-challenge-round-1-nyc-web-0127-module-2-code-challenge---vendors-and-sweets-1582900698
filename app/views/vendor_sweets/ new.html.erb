<h1> Connect Vendor and Sweets </h1>

<%= form_for @vendor_sweet do |f| %>
<%= f.collection_select :vendor_id, @vendorsweets, :id, :name%>
<%= f.collection_select :sweet_id, @sweets, :id, :name%>
<p> Enter Price: <%= f.number_field :price %> <p>
<%= f.submit %>
<% end %>

