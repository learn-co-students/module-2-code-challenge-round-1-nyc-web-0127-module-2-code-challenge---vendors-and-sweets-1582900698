<h1>All Vendors</h1>

<ul>
  <% @vendors.each do |vendor| %>
    <li><%= link_to  vendor.name, "/vendors/#{vendor.id}" %></li>
  <% end  %>
</ul>

<%= link_to  "Make a new association", "/vendor_sweets/new" %>